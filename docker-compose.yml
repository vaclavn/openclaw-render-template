services:
  openclaw:
    build: .
    container_name: vn-gbrain
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      PORT: 3000
