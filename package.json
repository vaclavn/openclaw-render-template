{
  "name": "vn-gbrain-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Deployable AlphaClaw image with git-crypt support for encrypted brain repo",
  "scripts": {
    "dev": "docker compose up --build",
    "dev:stop": "docker compose down",
    "dev:logs": "docker compose logs -f",
    "dev:shell": "docker compose exec openclaw bash"
  },
  "dependencies": {
    "@chrysb/alphaclaw": "0.9.9"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
